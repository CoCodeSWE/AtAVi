const Logic = require('../Client/Logic/Logic');
describe('Client', function()
{
	describe('Logic', function()
	{
		describe('Logic', function()
		{
			describe('sendData', function()
			{
				it('Se la promessa viene soddisfatta (fulfill) allora il metodo deve chiamare il metodo next del subject che si occupa di notificare gli Observer iscritti.');
				it('Se la promessa viene respinta (reject) allora il metodo deve chiamare il metodo error del subject che si occupa di notificare tale errore agli Observer iscritti.');
			});
		});
	});
});
