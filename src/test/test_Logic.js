const Logic = require('../Client/Logic/Logic');

describe('Client', function()
{
	describe('Logic', function()
	{
		describe('Logic', function()
		{
			describe('sendData', function()
			{
				it('Se la promessa viene soddisfatta (fulfill) allora devono essere notificati gli Observer iscritti.', function()
        {

        });

        it('Se la promessa viene respinta (reject) allora gli Observer iscritti devono essere notificati dell\'errore.', function()
        {

        });
			});
		});
	});
});
