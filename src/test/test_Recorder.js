const Manager = require('../Client/Recorder/Recorder');
const chai = require('chai');

describe('Client', function()
{
  describe('Recorder', function()
  {
    describe('Recorder', function()
    {
      describe('start', function()
      {
        it('Una volta chiamato il metodo \file{start}, viene inviata una serie di oggetti \file{RecorderMsg} a \file{RecorderWorker} con campo \file{command} uguale a “record” e questa serie di messaggi viene interrotta alla chiamata del metodo \file{stop}.');
      });

      describe('stop', function()
      {
        it('Una volta chiamato il metodo \file{start}, viene inviata una serie di oggetti \file{RecorderMsg} a \file{RecorderWorker} con campo \file{command} uguale a “record” e questa serie di messaggi viene interrotta alla chiamata del metodo \file{stop}.');
      });
    });
  });
});
